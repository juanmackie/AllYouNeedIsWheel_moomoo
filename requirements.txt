moomoo-api>=10.1.6108
pandas>=2.2.0
numpy>=1.26.4
cachetools>=5.3.0
pydantic>=2.0.0
python-dotenv>=1.1.0  # imported as `dotenv`
loguru>=0.7.2
pytz>=2024.1
yfinance>=0.2.28

# OpenBB SDK for financial data enrichment (free, no API keys needed)
openbb>=4.0.0

# FRED API for macro economic regime detection (free API key required)
fredapi>=0.4.2

# Web application dependencies
flask>=3.0.2
flask-cors>=4.0.0
gunicorn>=21.2.0
waitress>=2.1.2  # Windows-compatible WSGI server alternative to gunicorn
werkzeug>=3.0.1

# TradingView screener for dynamic stock screening (free, no API key)
tvscreener>=0.2.0

# OpenAI client for LLM-powered trade advisor
openai>=1.0.0

# Math/statistics dependencies
scipy>=1.12.0

# In-process scheduler
apscheduler>=3.10.0
